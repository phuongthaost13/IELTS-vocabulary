---
title: "Cambridge IELTS test books from 10 - 15 insights "
author: "Phạm Phương Thảo"
date: "8/27/2021"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r, warning=FALSE, message=FALSE}
library(here)
library(ggplot2)
library(dplyr)
```

**Số lượng các từ xuất hiện**

```{r word-number, dev='png', fig.cap="Số lượng các từ xuất hiện trong sách"}
ggplot(combined_df, aes(x = source, fill = source)) + geom_bar() + 
  scale_fill_brewer(palette = "Dark2")
```

```{r no-common-words, dev='png', fig.cap="Số lượng các từ xuất hiện trong sách không bao gồm tư thông dụng"}
ggplot(df_no_common_words, aes(x = source, fill = source)) + geom_bar() + 
  scale_fill_brewer(palette = "Dark2")
```